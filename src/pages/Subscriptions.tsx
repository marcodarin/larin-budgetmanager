/**
 * Abbonamenti e canoni ricorrenti. Struttura minima: il contenuto arriva col
 * blocco B7 lato interfaccia, il dominio nel database è già in piedi
 * (subscriptions, subscription_amounts, subscription_periods).
 */
const Subscriptions = () => (
  <div className="container mx-auto p-6">
    <h1 className="text-2xl font-bold">Abbonamenti</h1>
    <p className="mt-2 text-muted-foreground">Schermata in costruzione.</p>
  </div>
);

export default Subscriptions;
